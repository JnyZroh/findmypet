<div style="background-color: #046E8F;">
  <div class="banner mt-0" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://stlc.shopwhereilive.com/wp-content/uploads/2020/05/Screen-Shot-2020-05-12-at-6.00.56-PM.jpg);">
    <div class="container">
      <h1><strong>Find My Pet</strong>!</h1>
      <p>Look for your missing best friend.</p>
    </div>
  </div>

  <div class="container">
    <div class="d-flex justify-content-between mt-3 mb-3">
      <div class="d-grid gap-2 col-5 mx-auto">
        <%= link_to new_poster_path, class:"btn", style:"max-width: 200px; font-size:smaller; border-color: white; color: white; border-radius: 10px;" do %>
          <span style="font-weight:bold; font-size:200%;">＋</span><br>
          Lost My Pet
        <% end %>
      </div>
      <div class="d-grid gap-2 col-5 mx-auto">
        <%= link_to new_report_path, class:"btn", style:"max-width: 200px; font-size:smaller; border-color: white; color: white; border-radius: 10px;" do %>
          <span style="font-weight:bold; font-size:200%;">＋</span><br>
          Spotted a Pet
        <% end %>
      </div>
    </div>
  </div>

  <% if user_signed_in? %>
    <div class="container">
      <% current_user.pets.each do |pet| %>
        <% if pet.posters.any? %>
          <h2 style="color: white; font-weight:bold;">Your Poster</h2>
          <div class="row justify-content-center my-3">
            <% pet.posters.each do |poster| %>

              <div class="col-6">
                <div class="card" style="height: 30vh; border-radius: 20px; ">
                  <% photo_url = pet.photos.empty? ? 'https://www.vets4pets.com/globalassets/close-up-of-cat-looking-up.jpg?w=585&scale=down' : cl_image_path(pet.photos[0].key) %>
                  <img src="<%= photo_url %>" class="card-img-top rounded-bottom" style="max-height: 150px; object-fit: cover; border-radius: 20px;" alt="Pet photo">
                  <div class="card-body text-center">
                    <div>
                      <h5 class="card-title"><strong><%= poster.pet.name %></strong></h5>
                      <%= link_to edit_poster_path(poster.id), class:"btn", style:"padding: 2px 5px; background-color: #046E8F; color: white; border-radius: 10px; position:absolute; bottom: 43%; right: 5%" do %>
                      Edit
                      <% end %>
                    </div>
                    <p class="card-text">Last seen: <%= truncate(poster.address, :length => 15) %></p>
                    <p class="card-text"><strong><%= poster.date_missing %></strong></p>
                  </div>
                </div>
              </div>

              <div class="col-5">
                <h4  style="color: white;">Updates</h4>
                <div class="updates">No other updates yet!</div>
                <div class="updates">Your poster is live.</div>
                <div style="text-align:right"><%= link_to "View all", "/my_pets", class: "seeMoreLink"%></div>
              </div>

            <% end %>
          </div>
        <% end %>
      <% end %>
    </div>
<% end %>

  <div class="container">
  <h3 style="color: white; font-weight:bold;">Missing Pets Near You</h3>

      <% if @posters.any? %>
        <div class="row justify-content-center my-3">
          <% @posters.each do |poster| %>
            <% if poster.pet.user != current_user %>
              <div class="col-6 mb-2">
                  <%= link_to poster_path(poster.id) do %>
                <div class="card" style="height: 30vh; border-radius: 20px; ">
                  <%# <div style="height: 70%;"> %>
                    <% photo_url = poster.pet.photos.empty? ? 'https://www.vets4pets.com/globalassets/close-up-of-cat-looking-up.jpg?w=585&scale=down' : cl_image_path(poster.pet.photos[0].key) %>
                    <img src="<%= photo_url %>" class="card-img-top rounded-bottom" style="object-fit: cover; border-radius: 20px;" alt="Pet photo">
                  <%# </div> %>
                  <div class="card-body text-center">
                    <h5 class="card-title"><strong><%= poster.pet.name %></strong></h5>
                    <p class="card-text">Last seen: <%= truncate(poster.address, :length => 15) %></p>
                    <p class="card-text"><strong><%= poster.date_missing %></strong></p>
                  </div>
                </div>
              <% end %>
              </div>
            <% end %>

          <% end %>
        </div>
      <% end %>

    <div class="d-flex justify-content-end align-items-end">
      <a class="btn btn-ghost" href="#">See more</a>
    </div>
  </div>
</div>
